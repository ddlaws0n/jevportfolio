node_modules
.DS_Store
dist
dist-ssr
*.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.vercel
.claude/logs

# Secrets stay out. The example and the 1Password reference file carry no
# secret values, so they are tracked.
.env*
!.env.example
!.env.op
